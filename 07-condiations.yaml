- name: condiations
  hosts: local
  connection: local
  vars:
   number: 15
  tasks:
  - name: check number is less than 14 or not
    ansible.builtin.debug:
     msg: " given number : {{number}} is less then 14"
    when: number < 15